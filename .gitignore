# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/
env/

# Environment / secrets
.env
.env.*
*.pem

# Vector store / data artifacts
# data/raw/ (source PDFs) and data/faiss_index/ (built index) are
# intentionally tracked, not ignored — this is a small (~3.4MB), public
# corpus, and committing it means `git clone` + run works with no
# re-ingestion step. See README "Setup".
data/processed/

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store

# IDE
.vscode/
.idea/

# Docker
*.log
